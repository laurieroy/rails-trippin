<%= form_with(model: trip) do |form| %>
  <% if trip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trip.errors.count, "error") %> prohibited this trip from being saved:</h2>

      <ul>
        <% trip.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="flex flex-col">
    <%= form.label :purpose %>
    <%= form.rich_text_area :purpose %>
  </div>

  <div class="flex flex-col">
    <%= form.label :places %>
    <%= form.select :places, @places, {}, { multiple: false} %>
  </div>


  <div class="actions mt-4">
    <%= form.submit class: "px-2 py-1 bg-indigo-500 bg-opacity-75 text-indigo-50 rounded cursor-pointer" %>
  </div>
<% end %>
